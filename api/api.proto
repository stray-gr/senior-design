syntax = "proto3";

import "google/protobuf/empty.proto";
package api;

message Point {
    float x = 1;
    float y = 2;
}

service Query {
    rpc GetResults (google.protobuf.Empty) returns (stream Point) {}
}